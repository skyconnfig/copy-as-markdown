# Copy as Markdown 扩展程序测试指南

## 📦 扩展程序已打包完成

构建产物位于：`copy-as-markdown/distribution/`

## 🧪 功能测试清单

请按照以下步骤测试各项功能：

### 测试1：基础选中文本
1. 打开测试页面 `copy-as-markdown/test-page.html`
2. 选中「这是一段普通的测试文本」这段文字
3. 按 `Alt+X` 或右键选择「将选中文本复制为 Markdown」
4. 验证剪贴板内容：
   ```
   这是一段普通的测试文本，用于测试基本的文本复制功能。请选中这段文字进行测试。
   ```

### 测试2：格式化文本
| 功能 | 操作 | 期望结果 |
|------|------|----------|
| **粗体** | 选中「粗体文本」→ Alt+X | `这是一个**粗体文本**测试。` |
| **斜体** | 选中「斜体文本」→ Alt+X | `这是一个*斜体文本*测试。` |
| **删除线** | 选中「删除线文本」→ Alt+X | `这是一个~~删除线文本~~测试。` |
| **行内代码** | 选中「行内代码」→ Alt+X | `这是一个\`行内代码\`测试。` |

### 测试3：链接复制
1. 右键点击「这是一个指向 Google 的链接」
2. 选择「将链接复制为 Markdown」
3. 验证剪贴板内容：
   ```
   [这是一个指向 Google 的链接](https://www.google.com)
   ```

### 测试4：图片复制
1. 右键点击测试图片
2. 选择「将图片复制为 Markdown」
3. 验证剪贴板内容：
   ```
   ![测试图片](https://via.placeholder.com/150)
   ```

### 测试5：无序列表
1. 选中无序列表区域
2. 按 Alt+X
3. 验证剪贴板内容：
   ```
   - 第一项
   - 第二项
   - 第三项
   ```

### 测试6：有序列表
1. 选中有序列表区域
2. 按 Alt+X
3. 验证剪贴板内容：
   ```
   1. 有序第一项
   2. 有序第二项
   3. 有序第三项
   ```

### 测试7：表格
1. 选中表格区域
2. 按 Alt+X
3. 验证剪贴板内容包含 Markdown 表格格式：
   ```
   | 姓名 | 年龄 | 城市 |
   | ---- | ---- | ---- |
   | 张三 | 25   | 北京 |
   | 李四 | 30   | 上海 |
   | 王五 | 28   | 广州 |
   ```

### 测试8：代码块
1. 选中代码块内容
2. 按 Alt+X
3. 验证剪贴板内容：
   ````
   ```javascript
   function helloWorld() {
       console.log("Hello, World!");
       return true;
   }
   ```
   ````

### 测试9：标题
1. 选中「一级标题」「二级标题」等
2. 按 Alt+X
3. 验证输出为 Markdown 标题格式：
   ```
   # 一级标题

   ## 二级标题

   ### 三级标题
   ```

### 测试10：混合内容
1. 选中「混合内容」区域的所有内容
2. 按 Alt+X
3. 验证所有格式正确转换（粗体、斜体、链接、代码等）

## ✅ 测试通过标准

| 测试项 | 通过标准 |
|--------|----------|
| 基础文本 | 原文复制，无格式丢失 |
| 粗体文本 | 包含 `**` 标记 |
| 斜体文本 | 包含 `_` 或 `*` 标记 |
| 删除线 | 包含 `~~` 标记 |
| 行内代码 | 包含反引号 `` ` `` |
| 链接 | Markdown 链接格式 `[text](url)` |
| 图片 | Markdown 图片格式 `![alt](url)` |
| 列表 | 无序列表用 `-`，有序列表用 `1.` |
| 表格 | 包含 `|` 分隔符和 `---` 对齐行 |
| 代码块 | 围栏代码块格式 ``` ``` |

## 📋 测试记录表

| 测试项 | 操作 | 期望结果 | 实际结果 | 通过 |
|--------|------|----------|----------|------|
| 基础文本 | 选中→Alt+X | 原文复制 | | ☐ |
| 粗体 | 选中→Alt+X | `**粗体**` | | ☐ |
| 斜体 | 选中→Alt+X | `*斜体*` | | ☐ |
| 删除线 | 选中→Alt+X | `~~删除~~` | | ☐ |
| 行内代码 | 选中→Alt+X | `` `code` `` | | ☐ |
| 链接 | 右键菜单 | `[text](url)` | | ☐ |
| 图片 | 右键菜单 | `![alt](url)` | | ☐ |
| 表格 | 选中→Alt+X | Markdown表格 | | ☐ |
| 代码块 | 选中→Alt+X | ``` ``` ``` | | ☐ |
| 列表 | 选中→Alt+X | Markdown列表 | | ☐ |

## 🐛 已知问题

1. **Chromium 浏览器限制**：Chrome 无法提取链接文本和图片 alt 文本，会使用链接 URL 代替
2. **Iframe 内容**：无法复制 iframe 内的内容（安全限制）
3. **非 HTTPS 页面**：在不安全页面（http://）可能无法复制到剪贴板
4. **用户激活**：必须先与页面交互才能使用剪贴板 API

## 📞 测试反馈

如果发现任何问题，请记录：
- 测试环境（浏览器版本、操作系统）
- 测试步骤
- 期望结果
- 实际结果
- 截图或录屏（如有）
